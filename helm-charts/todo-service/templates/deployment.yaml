apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: todo-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: todo-service
    app.kubernetes.io/name: todo-service
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: todo-service
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  replicas: {{ .Values.deployment.replicas }}
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: todo-service
{{- if .Values.canary.enabled }}
  strategy:
    canary:
      canaryService: todo-service-canary
      stableService: todo-service
      steps:
      - setWeight: 20
      - analysis:
          templates:
          - templateName: {{ .Values.canary.analysisTemplateName }}
          args:
          - name: service-name
            value: todo-service-canary
          - name: port
            value: "{{ .Values.service.port }}"
      - pause: {}
      - setWeight: 40
      - pause: { duration: 10s }
      - analysis:
          templates:
          - templateName: {{ .Values.canary.analysisTemplateName }}
          args:
          - name: service-name
            value: todo-service-canary
          - name: port
            value: "{{ .Values.service.port }}"
      - setWeight: 60
      - pause: { duration: 10s }
      - analysis:
          templates:
          - templateName: {{ .Values.canary.analysisTemplateName }}
          args:
          - name: service-name
            value: todo-service-canary
          - name: port
            value: "{{ .Values.service.port }}"
{{- end }}
  template:
    metadata:
      labels:
        app: todo-service
        app.kubernetes.io/name: todo-service
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: todo-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.service.targetPort }}"
        prometheus.io/path: "/metrics"
    spec:
{{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecrets.name }}
{{- end }}
      containers:
      - name: todo-service
        image: {{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.service.targetPort }}
        env:
        - name: SECRET_KEY
          value: "{{ .Values.env.secretKey }}"
        - name: USER_SERVICE_URL
          value: "{{ .Values.env.userServiceUrl }}"
        - name: DATABASE_URL
          value: "{{ .Values.env.databaseUrl }}"
{{- if .Values.otel.enabled }}
        # OpenTelemetry Configuration
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "{{ .Values.otel.endpoint }}"
        - name: OTEL_EXPORTER_OTLP_PROTOCOL
          value: "{{ .Values.otel.protocol }}"
        - name: OTEL_SERVICE_NAME
          value: "todo-service"
        - name: OTEL_RESOURCE_ATTRIBUTES
          value: "service.namespace={{ .Release.Namespace }},service.version={{ .Values.image.tag }},deployment.environment={{ .Release.Namespace }}"
        - name: OTEL_TRACES_EXPORTER
          value: "otlp"
        - name: OTEL_METRICS_EXPORTER
          value: "none"
        - name: OTEL_LOGS_EXPORTER
          value: "none"
{{- end }}
        livenessProbe:
          httpGet:
            path: {{ .Values.probes.liveness.httpGet.path }}
            port: {{ .Values.probes.liveness.httpGet.port }}
          initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
          periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
          timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
        readinessProbe:
          httpGet:
            path: {{ .Values.probes.readiness.httpGet.path }}
            port: {{ .Values.probes.readiness.httpGet.port }}
          initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
          periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
{{- with .Values.resources }}
        resources:
          {{- toYaml . | nindent 10 }}
{{- end }}
{{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
{{- end }}
{{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
{{- end }}
{{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
{{- end }}
